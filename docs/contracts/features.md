# Feature Contracts

The formal feature contract lives in the validated_feature_contract.json output of the `validate` command, generated each run to `reports/YYYY-MM-DD/validated_feature_contract.json`.

This document provides context and explains the contract's structure, evolution, and relationship to the legacy Wellington contracts.

## Historical Origin

The contracts in `quarentena/Wellington/contracts/` (Phase 1 baseline, frozen 2026-05-29) defined the old project's feature schema. Key documents:

- **features.md:** Feature inventory v0.1 with hard rules (causal timestamp, closed='left', no `tmpf` for `ts >= cp_utc`).
- **objective.md:** Trading objective function v1.0 (max EV, subject to drawdown/coverage/trade/spike constraints).
- **imputation.md:** Missing value handling rules (carried forward into SolarStorm's `quality` tag system).
- **quantization.md:** Temperature binning rules (superseded by SolarStorm's decimal-internal integer-output design).
- **resolver.md:** Contract resolution rules (superseded by `_settlement.py`).

The old trading objective is historical context only. SolarStorm's current
scope is model-first: EV, market pricing, position sizing, shadow trading, and
Polymarket API work remain on hold until a production model passes its gates.

## Current Contract Structure

The `validated_feature_contract.json` is generated by `validate_hypotheses()` and contains:

```json
{
  "validated_features": [
    {
      "id": "H1",
      "feature_column": "slope_3h",
      "cp": "20:00",
      "regime": "all",
      "effect_size": 0.123,
      "ci_lo": 0.045,
      "ci_hi": 0.201,
      "p_value": 0.002,
      "best_null_name": "calibrated_cp_mean_rw",
      "best_null_mae": 1.234
    }
  ],
  "blocked": [
    {
      "id": "H19",
      "feature_column": "sst_maritime_cap",
      "reason": "Requires Cook Strait SST -- no METAR source available (H19)"
    }
  ],
  "generated": "2026-06-05T12:00:00+00:00",
  "alpha": 0.05,
  "n_hypotheses_tested": 88,
  "n_validated": 0,
  "n_rejected": 88
}
```

### Validated Features

A feature is **validated** when:
1. Walk-forward bootstrap 95% CI excludes zero (CI low > 0.0).
2. Benjamini-Hochberg FDR correction at alpha=0.05 survives.
3. All five gates (G1-G5) pass.

Each validated entry includes:
- `id`: Hypothesis ID (H1-H23)
- `feature_column`: Column name in `features.parquet`
- `cp`: Checkpoint hour (20:00, 21:00, 22:00, 23:00)
- `regime`: "all" or a specific causal physical regime. The old
  `late_warming` regime is deprecated because it is an ex-post timing outcome,
  not a causal pre-CP regime.
- `effect_size`: Mean error reduction vs baseline (positive = better)
- `ci_lo`, `ci_hi`: 95% bootstrap confidence interval bounds
- `p_value`: One-sided bootstrap p-value for H0: mean(baseline) <= mean(challenger)
- `best_null_name`: Winning train-only null baseline for that CP/result slice
- `best_null_mae`: MAE of the winning null on the exact same validation days

The best null is selected inside the walk-forward validation loop. It may be
`L0_persistence`, `L1_dminus1`, `L2_climatology_doy`,
`L4_empirical_conditional`, or `calibrated_cp_mean_rw`, depending on which
train-only null wins for the CP and validation slice.

### Blocked Features

Features listed in `BLOCKED_FEATURES` dict in `solarstorm/features/builder.py`. Currently:

| Feature              | Reason                                                        |
|----------------------|---------------------------------------------------------------|
| `sst_maritime_cap`   | Requires Cook Strait SST -- no METAR source available (H19)   |

### Feature Schema

All feature columns in `features.parquet`:

| Column                        | H-ID | Description                                          |
|-------------------------------|------|------------------------------------------------------|
| `date_local`                  | --   | Local date (index)                                   |
| `cp`                          | --   | Checkpoint hour string (index)                       |
| `regime_label`                | H3   | Causal physical regime at CP; see `docs/regime_model_card.md` |
| `regime_flags`                | --   | JSON with per-day regime flags                       |
| `slope_3h`                    | H1   | Warming rate (C/h) between earliest/latest anchors   |
| `hours_to_expected_peak`      | H2   | Hours from CP to climatological Tmax time            |
| `dewpoint_depression`         | H4   | Mean (T - Td) at CP                                  |
| `tmax_dminus1`                | H5   | Previous day's Tmax                                  |
| `tmin_delta_tmax`             | H6   | Tmin - Tmax(D-1)                                     |
| `intraday_regime_change`      | H7   | Provisional boolean: observed day-state characteristics changed within day |
| `wind_dir_change_s_to_n`      | H8   | Wind direction shift S -> N (degrees)                |
| `day_sequence_pattern`        | H9   | Recent-day pattern: flat/warming/cooling/peaked/troughed |
| `precip_disruption`           | H10  | Flag: precipitation present                          |
| `tmax_hour_by_regime_month`   | H11  | Provisional; must be rebuilt with train-only physical-regime timing norms |
| `cloud_cover_suppression`     | H12  | Maximum cloud cover weight (0-1)                     |
| `pressure_trend_3h`           | H13  | QNH trend (hPa/h), possible day-state change signal  |
| `foehn_score`                 | H14  | NW flow strength x dewpoint depression               |
| `late_warming_anomaly`        | H15  | Superseded concept; late Tmax must be month/regime-relative timing risk |
| `regime_score_argmax`         | H16  | Current physical-regime label; must still earn validation |
| `warming_rate_06_09`          | H17  | Morning warming rate (C/h) T09-T06 / 3               |
| `nocturnal_plateau_flag`      | H18  | Peak-already-passed detector flag                    |
| `sst_maritime_cap`            | H19  | BLOCKED -- Cook Strait SST not available             |
| `dewpoint_collapse_rate_3h`   | H20  | Dewpoint drop rate (C/h)                             |
| `prefrontal_warming_window`   | H21  | Falling QNH + dry + N/NW flow flag                   |
| `nw_sector_not_foehn`         | H22  | NW/W flow crossing cold Cook Strait (false foehn)    |
| `cloud_base_transparency`     | H23  | Cloud suppression weighted by base height            |

## Contract Versioning

The feature contract is regenerated each time `tmax validate` runs. Features that pass validation in one run may fail in a later run if:
- More training data becomes available (changing the walk-forward splits).
- Feature computation logic changes.
- New baselines shift the null comparison.

The contract is versioned by the generation timestamp in the `generated` field.

## Regime-Dependent Feature Quarantine

The 2026-06-06 Onda 4 NO-GO showed that the old regime ontology mixed causal
states with ex-post Tmax timing. Onda 2R repaired the code path; however,
regime-dependent features remain provisional because the post-Onda 2R
robustness rerun still blocks on dead physical regimes (`calm_radiative` and
`standard_nw`):

- `regime_label`
- `regime_score_argmax`
- `tmax_hour_by_regime_month`
- `late_warming_anomaly`
- `intraday_regime_change` and future transition-risk features until the base
  physical regimes are validated
- any future month/regime timing feature

## Comparison with Legacy Contracts

The old project's `features.md` contract (v0.1, frozen 2026-05-29) defined 18 features organized by source. SolarStorm carries forward the hard rules (causal timestamp, closed='left', no future leakage) and the feature inventory concept, but:

- Feature IDs are linked to testable hypotheses (H1-H23), not a static inventory.
- Features are validated via walk-forward bootstrap CI, not design review.
- The contract is machine-generated (JSON), not a static markdown document.
- Blocked features are explicitly tagged with reasons.

The legacy contracts remain in `quarentena/Wellington/contracts/` for historical reference.
